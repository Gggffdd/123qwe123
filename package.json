{
  "name": "universal-shop",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"cd api && python -m uvicorn app:app --reload --port 8000\" \"cd frontend && npm run dev\"",
    "build": "cd frontend && npm run build",
    "start": "cd frontend && npm start",
    "bot": "cd api && python bot.py"
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  }
}
